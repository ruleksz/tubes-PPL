name: laravel-tests

on:
  push:
    branches: ["master"]
  pull_request: 
    branches: ["master"]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, xml, pcntl, pdo_mysql
          coverage: xdebug

      - name: Install Dependencies
        working-directory: nasa-app
        run: composer install --no-interaction --prefer-dist

      - name: Copy .env.example to .env
        working-directory: nasa-app
        run: cp .env.example .env

      - name: Generate App Key
        working-directory: nasa-app
        run: php artisan key:generate

      - name: Fail if JS files exist
        run: |
            if git diff --name-only HEAD origin/master | grep ".js"; then
              echo "❌ Tidak boleh file selain .php"
              exit 1
            fi
      - name: Validate Uploaded Files
        run: |
          echo "🔎 Checking changed files..."
      
          # Ambil file yang berubah
          CHANGED_FILES=$(git diff --name-only HEAD~1)
      
          echo "$CHANGED_FILES"
      
          # Flag error
          HAS_ERROR=0
      
          for FILE in $CHANGED_FILES; do
            # Skip folder tertentu
            if [[ "$FILE" == vendor/* || "$FILE" == storage/* || "$FILE" == public/* || "$FILE" == node_modules/* ]]; then
              continue
            fi
      
            # Jika file kosong
            if [ ! -s "$FILE" ]; then
              echo "❌ ERROR: File kosong terdeteksi → $FILE"
              HAS_ERROR=1
            fi
          done
      
          if [ "$HAS_ERROR" -eq 1 ]; then
            echo "❌ Build FAILED karena ada file kosong yang diupload."
            exit 1
          fi
      
          echo "✔ Semua file valid."


      - name: Run Tests
        working-directory: nasa-app
        run: php artisan test
