name: laravel-tests

on:
  push:
    branches: ["master"]
  pull_request: 
    branches: ["master"]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, xml, pcntl, pdo_mysql
          coverage: xdebug

      - name: Install Dependencies
        working-directory: nasa-app
        run: composer install --no-interaction --prefer-dist

      - name: Copy .env.example to .env
        working-directory: nasa-app
        run: cp .env.example .env

      - name: Generate App Key
        working-directory: nasa-app
        run: php artisan key:generate

      - name: Fail if JS files exist
        run: |
            if git diff --name-only HEAD origin/master | grep ".js"; then
              echo "❌ Tidak boleh file selain .php"
              exit 1
            fi
      - name: Validate Uploaded Files
        run: |
          echo "🔎 Checking changed files..."

          # Cek apakah HEAD~1 ada
          if git rev-parse HEAD~1 >/dev/null 2>&1; then
            CHANGED_FILES=$(git diff --name-only HEAD~1)
          else
            echo "⚠ HEAD~1 tidak ditemukan, memakai file dari commit terakhir."
            CHANGED_FILES=$(git ls-files)
          fi
          
          echo "$CHANGED_FILES"
          
          HAS_ERROR=0
          
          for FILE in $CHANGED_FILES; do
            # Skip folder
            if [[ "$FILE" == vendor/* || "$FILE" == storage/* || "$FILE" == public/* || "$FILE" == node_modules/* ]]; then
              continue
            fi
          
            # Validasi file kosong
            if [ ! -s "$FILE" ]; then
              echo "❌ ERROR: File kosong terdeteksi → $FILE"
              HAS_ERROR=1
            fi
          done
          
          if [ "$HAS_ERROR" -eq 1 ]; then
            echo "❌ Build FAILED karena ada file kosong."
            exit 1
          fi
          
          echo "✔ Semua file valid."



      - name: Run Tests
        working-directory: nasa-app
        run: php artisan test
